# hoist peer deps of vue/react plugins so consumers can share single copy
public-hoist-pattern[]=vue*
public-hoist-pattern[]=react*

# strict peer dep resolution to surface version conflicts early
strict-peer-dependencies=false
auto-install-peers=true

# prevent accidental publish from monorepo root
enable-pre-post-scripts=true
